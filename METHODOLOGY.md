# 方法论：把科学方法用在基准测试上

本仓库的所有专题都按这份文档执行。它脱胎于[科学方法](https://zh.wikipedia.org/zh-cn/%E7%A7%91%E5%AD%A6%E6%96%B9%E6%B3%95)
的基本原则 —— 提出可证伪的假说、推导出具体预测、设计实验检验、结果必须可重复、数据和程序充分公开。

和自然科学一样，这不是一套必须按顺序走完的流程，而是一组约束。但有一条是硬性的：
**假说必须写在跑基准之前**，否则就是看着数据编解释。

---

## 十一条原则

### 1. 问题指向机制，不指向排名

> ❌ "`fmt.Sprintf` 和 `strconv.Itoa` 哪个快"
> ✅ "`fmt.Sprintf` 多出来的那次分配是从哪来的"

前者的答案是一个数字，换个场景就失效；后者的答案是"`...any` 装箱 + fmt 把参数交给反射导致编译器判定泄漏"，
这个机制能迁移到所有可变参数 + 反射的 API 上。

**只有机制层面的问题，答案才有迁移价值。**

### 2. 假说必须可证伪，且写在实验之前

假说要写成能被实验推翻的形式：

> ❌ "用指针更高效" —— 没有边界条件，怎么测都能自圆其说
> ✅ "`NewPointByPointer` 比 `NewPointByValue` 多一次堆分配，因为返回的指针指向自己的栈帧，
>    违反逃逸分析的第二条不变量"

后者只要跑出 `0 allocs/op` 就当场被推翻。**不可能被推翻的说法不值得写进文档。**

顺序也是硬约束。先跑数据再补假说，人会不自觉地把任何结果都解释成"本来就该这样"，
这正是确认偏误发生的地方。专题 README 里的「假说」一节应当在动手跑 `make bench` 之前定稿。

### 3. 预测落到硬指标上，`ns/op` 不算

从假说推导出的预测必须比"更快"更具体。按可信度排序：

| 指标 | 性质 | 怎么拿 |
|---|---|---|
| `allocs/op` / `B/op` | 确定性，跨机器可比 | `make bench` |
| 逃逸分析输出 | 编译器直接给出的判断 | `make escape` / `make escape-inline` |
| 栈帧大小 `locals=0x…` | 编译产物的客观事实 | `make frames` / `make asm` |
| `ns/op` | 受机器状态影响，只在同一次运行内可比 | `make bench` |

预测写成「`allocs/op` 从 0 变 1，且 `-m` 会打印 `escapes to heap`」，
不要写成「会慢一点」。

### 4. 对照实验一次只动一个变量

理想形态是两个函数只差一行：

```go
func SumLeakedPointer() int {
	p := newPointInlinable()
	escapedPoint = p        // ← 唯一的差异
	return p.X + p.Y
}
```

工作量也必须等价 —— 比较 `make([]byte, 64)` 和 `make([]byte, n)` 时，实参必须恒为 64，
否则测的是两件事。**同时改了两个变量的对照组，差值无法归因。**

### 5. 可重复性：单次结果不是数据

- `-count` ≥ 8（benchstat 需要 ≥6 个样本才给置信区间）
- 用 benchstat 看中位数和波动率，不盯单次输出
- 跑之前关掉编译、下载、构建等后台任务
- 文档里必须写出波动率，`±27%` 本身就是"这个数字别拿来做精细比较"的警告

实例：同一份代码 `Pointer_Returned` 一次跑出 30ns，重跑是 7.87ns，差 3.8×，纯粹是后台负载。
只跑一次的话，文档里就会有一个错误 4 倍的数字。

### 6. 先量出自己的分辨率，再谈差异

一个 3% 的差距是信号还是噪声？**没有分辨率就没法回答。**
benchstat 的置信区间给的是同一个基准自身的波动，不告诉你两个不同基准之间
多大的差值才算真的不同 —— 后者还包含代码布局、缓存对齐、调度这些系统性偏差。

做法很简单：**在每个专题里留一对函数体逐字相同的基准**，当噪声地板。

```go
//go:noinline
func AppendPrealloc(n int) []int { s := make([]int, 0, n); /* … */ }

// 函数体和上面逐字相同。这不是冗余，是噪声地板对照组。
//go:noinline
func FillByAppend(n int) []int { s := make([]int, 0, n); /* … */ }
```

专题 02 实测这一对差 **6.4%**。有了这个数字，同一批数据里 append 和索引赋值
之间那 1% 的差距就当场结案了 —— 完全相同的代码都能差 6.4%，1% 不是信号。

**小于噪声地板的差异，不要解释它，不要写进结论，也不要据此改代码。**
真要区分这么小的差距，只能提高实验强度（增大 `-count`、换更安静的机器、
用 `perf`/`pprof` 换一个测量维度），而不是盯着 `ns/op` 讲故事。

**噪声地板不是机器的常数，是那一批数据的属性。**

它随 `-count`、`-benchtime`、机器当时的状态、甚至并发度变。两个实测：

| 专题 | 变量 | 噪声地板 |
|---|---|---|
| 03 | `-count=8`，开着浏览器 | 9.8% |
| 03 | `-count=15`，机器闲下来 | **1.2%** |
| 15 | `-cpu=1` / `2` / `4` / `10` | 9.9% / **30%** / **46%** / 0.1% |

同一台机器、同一对逐字相同的函数，专题 03 两批差 8 倍。
专题 15 更糟：它连**单调**都不是 —— 两端都很干净，最吵的是中间两档。
所以也别指望"跑满核就准了"这种直觉。

所以那对函数要**跟着每一批数据一起跑**，文档里按批报告 ——
换了 `-count`、换了并发度、隔天重跑，都要重新量。
拿一个地板走天下，等于把最吵那批的结论用最静那批的标准去解读。

反过来，这个数字也是"要不要重跑"的判据：专题 03 的场景四和场景六第一轮
跑出 ±34%、±54% 的波动，地板 9.8% 根本压不住，只能换参数重来。
没有这一步，那两个场景会得出错误结论。

### 7. 先确认实验测到的真是你以为的东西

这条是基准测试特有的，物理实验里没有对应物：**被测对象可能根本不存在。**
编译器会把没有可观测副作用的计算整个删掉，而计时器不会告诉你这件事。

规矩：**任何"快得离谱"的数字，先 `make frames` / `make asm` 确认代码还在。**

`b.Loop()` 只保证循环体内**函数调用**的参数和返回值被保活，管不到被内联展开成纯算术的代码。

### 8. 三条证据链必须互相吻合

一个结论要同时拿到三样东西，且互相印证：

1. **基准数字** —— `allocs/op` 确实变了
2. **编译器证据** —— `-m` 说 `escapes to heap`，`-S` 显示栈帧确实变小
3. **源码机制** —— 在 runtime / 编译器源码里找到对应的实现（常量、分支、数据结构）

三者对不上，说明假说错了或实验设计错了。**不能挑一个好看的写进文档。**

### 9. 充分公开

- 环境写全：机器 / OS / arch / Go 版本 / `-count`
- 原始基准输出留在 `.bench/`，不只留汇总
- 被测代码在仓库里，一行命令复现
- 文档里每个数字都要能指到产生它的那条命令

### 10. 如实记录被推翻的假说和踩过的坑

最容易偷懒的一条。实验翻车之后修好、当无事发生，是对读者信息量最大的一次删除 ——
**推翻的过程比正确答案更有用**，它告诉读者这个错误长什么样、怎么识别。

所以每个专题正文都有「坑」那一节。没有翻车不用硬编，但真翻了必须写。

### 11. 结论标注适用边界

不外推到没测过的地方：

- 分清**语言规范**和**编译器实现细节**（栈变量阈值属于后者，还受 `-smallframes` 影响）
- 结论绑定版本和架构（Go 1.24 换 Swiss Table 之后，map 的旧结论直接作废）
- 别在实验里制造现实中不存在的条件。两个例子，后一个难发现得多：
  - 用 `strconv.Itoa(1)` 测整数格式化会得到"零分配"，因为小整数走静态表 ——
    这个条件在源码里看得见，`1` 是个字面量
  - 用 `keys[i] = int64(i)` 当 map 的 key 会得到一个**恰好均衡**的哈希分布
    （专题 03 坑二：0..3583 分成 4 份是 896/896/896/896，一个不差），
    于是预分配显得比实际管用 —— 而这一行看上去毫无倾向性

  后者的教训是：**输入生成器是实验的一部分。**
  凡是结论依赖数据分布的实验（预分配、扩容、分片、负载均衡、缓存命中率），
  都要换一种分布再跑一遍。专题 03 的做法是所有预分配基准都带 `seq` 和 `rand` 两条线。

---

## 专题 README 的骨架

**这十一条原则约束的是怎么做实验，不是怎么写文档。**
文档面向的是想学 Go 的人，不是复现实验的审稿人 —— 主线必须是教程，
严谨性收进折叠块和附录，让它可查但不挡路。

```
标题 + 钩子      一段话 + 一个最小对照 + 一组数字，让人立刻看到差异有多大
目录

背景            这个机制为什么值得关心（成本模型 / 底层规则）
工具            怎么问编译器/runtime 要证据，读者后面一直会用

场景 1…N        主体。每个场景固定四拍：
                  常见写法 → 实测数字 → **为什么**（机制）→ **怎么用**（实践）
                <details>证据链 / 拆解验证</details>  ← 想深挖的人才展开

一页速查         表格：你写的 → 结果 → 更好的写法
坑               翻车记录。这部分反而要放在正文，它最有教学价值

附录 A 完整数据与环境（原则 5、6、9）
附录 B 实验设计：控制了什么变量、噪声地板多大、假说是不是事前写的（原则 2、4、6）
附录 C 延伸阅读：runtime / 编译器源码位置
```

**哪些东西必须进正文、哪些收进折叠块：**

| 进正文 | 收进 `<details>` 或附录 |
|---|---|
| 现象、代码对照、关键数字 | 完整 benchstat 表格 |
| 机制解释（为什么） | 三条证据链的对账 |
| 实践建议（怎么用） | 假说的原始措辞、命中情况 |
| 踩过的坑 | 对照组控制了哪些变量 |
| 噪声地板是多少（一句话） | 噪声地板对照组的完整数据 |
| 适用边界（一句话带过） | 环境细节 |

假说和预测**仍然必须在跑基准之前写下来**（原则 2），只是它们的归宿是
`xxx_test.go` 里 `Verify_` 组的注释 + 附录 B，而不是正文的第二节。

## 写一个专题的流程

```bash
# 1. 先写假说和预测 —— 写在 xxx_test.go 的 Verify_ 组注释里，此时还没有任何数据
# 2. 写被测代码和基准，其中至少一对函数体逐字相同，用作噪声地板（原则 6）
make check   TOPIC=<topic>

# 3. 先验证实验有效性，再看性能数字（原则 7）
make frames  TOPIC=<topic>          # 代码还在吗
make escape  TOPIC=<topic>          # 编译器的判断和预测一致吗
#    顺便问一句：输入是真实分布吗（原则 11）—— 顺序整数 key、常量实参、
#    单一 key 的并发基准，都会造出现实中不存在的条件

# 4. 跑数据
make bench   TOPIC=<topic>          # -count=8 + benchstat

# 5. 先看噪声地板那一对差多少，得到这批数据的分辨率（原则 6）
#    地板压不住要下的结论，就换参数重跑（加 -count、等机器闲），别硬解读
# 6. 逐条核对预测命中还是被推翻。小于噪声地板的差异一律按"无差异"处理。
#    被推翻的不要偷偷改假说 —— 写进「坑」那一节，那是全文最有价值的部分
# 7. 最后才写 README。此时你已经知道结论了，按教程的顺序组织，不要按实验的顺序
```

---

## 自查清单

提交一个专题前逐条过：

- [ ] 假说是在跑基准之前写的，原始措辞保留在 `Verify_` 组的注释里
- [ ] 每条假说都能指出一个"如果观察到 X 就说明我错了"的 X
- [ ] 正文读起来是教程不是报告：每个场景都有"为什么"和"怎么用"两拍
- [ ] 每组对照只有一个变量不同，工作量等价
- [ ] 跑了 `-count` ≥ 8，文档里有波动率
- [ ] 有一对函数体逐字相同的噪声地板对照组，**每一批数据都跑了一次**，文档里按批写出差多少
- [ ] 所有被解释成"差异"的数字都大于**它所在那批**的噪声地板
- [ ] 结论依赖数据分布的话，换一种分布重跑过
- [ ] 所有"零分配"和"快得离谱"的结论都用 `make frames` / `make asm` 验证过代码没被删
- [ ] 每个结论都有基准数字 + 编译器证据 + 源码机制三条链
- [ ] 环境信息完整，命令可复现
- [ ] 翻车和被推翻的假说如实写进「坑」那一节
- [ ] 结论标了版本、架构、失效条件
